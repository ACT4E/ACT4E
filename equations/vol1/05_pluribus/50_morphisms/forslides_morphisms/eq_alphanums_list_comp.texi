\begin{equation*}
        {}
        \asciienc( \texttt{A} \mtimes  \texttt{C} \mtimes \texttt{T})
        =\asciienc(\texttt{A}) \mtimes   \asciienc(\texttt{C}) \mtimes \asciienc(\texttt{T})\end{equation*}
