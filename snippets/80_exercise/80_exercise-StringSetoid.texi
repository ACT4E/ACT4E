% original size: lines = 15 line width = 65
% bounds: min_size = 0 max_size = 120 min_lines = 0
% transformed to maxlen = 65
\begin{minted}[mathescape, breaklines]{python}
class StringSetoid(Setoid[str]):
  """Represents the setoids of strings."""

  def contains(self, x: str) -> bool:
    return isinstance(x, str)

  def save(self, _: IOHelper, x: str) -> str:
    if not isinstance(x, str):
      raise InvalidValue("StringSetoid.save: x is not a string")
    return x

  def load(self, _: IOHelper, o: str) -> str:
    if not isinstance(o, str):
      raise InvalidFormat("StringSetoid.load: x is not a string")
    return o
\end{minted}