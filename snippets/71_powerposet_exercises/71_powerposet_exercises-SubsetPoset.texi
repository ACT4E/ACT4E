% original size: lines = 9 line width = 82
% bounds: min_size = 0 max_size = 120 min_lines = 0
% transformed to maxlen = 82
\begin{minted}[mathescape, breaklines]{python}
class SubsetPoset(Poset, ABC):
  """ A poset of subsets. """

  def construct(self, elements: List[Element]) -> Element:
    """ Given a list of elements, builds the element that represents the poset."""
    pass

  def contents(self, e: Element) -> List[Element]:
    """ Returns the contents of a subset"""
\end{minted}