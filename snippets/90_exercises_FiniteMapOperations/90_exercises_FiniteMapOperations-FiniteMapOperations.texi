% original size: lines = 8 line width = 85
% bounds: min_size = 0 max_size = 120 min_lines = 0
% transformed to maxlen = 85
\begin{minted}[mathescape, breaklines]{python}
class FiniteMapOperations(ABC):
  @abstractmethod
  def identity(self, s: FiniteSet[A]) -> FiniteMap[A, A]:
    """Returns the identity on a set."""

  @abstractmethod
  def compose(self, f: FiniteMap[E1, E2], g: FiniteMap[E2, E3]) -> FiniteMap[E1, E3]:
    """Compose two functions."""
\end{minted}