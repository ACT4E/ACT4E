\begin{minted}{python}
class FiniteMonoidRepresentation(ABC):
    def load(cls, s: str) -> FiniteMonoid:
        """ Load the data  """

    def save(cls, m: FiniteMonoid) -> str:
        """ Save the data  """
\end{minted}