% original size: lines = 14 line width = 87
% bounds: min_size = 0 max_size = 120 min_lines = 0
% transformed to maxlen = 87
\begin{minted}[mathescape, breaklines]{python}
class CurrencyOptimization:
  @abstractmethod
  def compute_optimal_conversion(
    self,
    available: SemiCategory[
      RichObject[str],
      RichMorphism[CurrencyExchanger]
    ],
    source: str,
    amount: float,
    target: str,
  ) -> OptimalSolution:
    """ Returns the optimal path for converting a certain amount from source to target.
      Raises InvalidValue if there is no path. """
\end{minted}