\begin{minted}{python}
class FiniteMapRepresentation(ABC):
    def load(cls, s: str) -> FiniteMap:
        """ Load the data  """

    def save(cls, m: FiniteMap) -> str:
        """ Load the data  """
\end{minted}