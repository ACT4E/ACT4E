% !TEX root = ../ACT4E-devel-fast.tex
% Node: these are commands that cannot go in the preamble.
% In here, you can assume to have the boolean sets

%\pagestyle{scrheadings}
%\ohead{\pagemark}
%\ihead{\headmark}
%\cfoot{}

%\dominitoc
%\doparttoc
%\setcounter{parttocdepth}{0}
%\setcounter{minitocdepth}{1}
%\setcounter{tocdepth}{1}

\def\parttoc{{\color{red} disabled because of kaobook}}
\def\minitoc{{\color{red} disabled because of kaobook}}

%\ifthenelse{\boolean{statuscolors}}{}{
%%\def\readytoreview{}
%%\def\statusdraft{}
%%\def\statusmissing{}
%}

\ifthenelse{\boolean{devel}}{}{
\def\XXX{
\PackageError{ACT4E}{You have included XXX in a non-devel version (devel = \boolean{devel}).}{We interrupt compilation.}
}

}

\newenvironment{forslides}{\colorlet{shadecolor}{Pink!50}\begin{shaded*}%
\textbf{This content was created for slides. Do not erase or change the names}.\\%
                                      }{\end{shaded*}}


%
%\ifthenelse{\boolean{devel}}{
%\let\oldsection\section
%\renewcommand\section{\FloatBarrier\clearpage\oldsection}
%}{}

\let\oldsection\section
\renewcommand\section{\FloatBarrier\clearpage\oldsection}
