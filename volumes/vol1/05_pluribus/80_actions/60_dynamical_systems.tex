% !TEX root = chapter-standalone.tex

\section{Dynamical systems}
\label{sec:dynamical-systems}

Dynamical systems are ubiquitous in engineering, and there are various ways to formulate this concept mathematically.
At a very basic level, there are usually the following ingredients:
\begin{enumerate}
    \item a \emph{state space} which comprises the possible states of the system in question
    \item a \emph{dynamics rule} which governs how states of the system may change
\end{enumerate}
Typically, there is a model of \emph{time} involved (e.g. either in discrete time-steps, or as continuous time) for describing how states change over time, or, instead of a model of time, one may use a set of \emph{events} as the triggers for changes of state.

A common way to model dynamics is by a right action operation of a monoid $\mathbf{T} = \tup{\stylesets{T}, \mthen_{\mathbf{T}}, \idmon_{\mathbf{T}}}$ on a set $\stylesets{X}$ which models the state space,
\begin{equation}
    \stylefunctors{\aword{dyn}} \colon  \stylesets{X} \cartprod \stylesets{T} \sto \stylesets{X}.
\end{equation}

\devel{
    \subsection{A simplest dynamical system}
    The following is a very simple example of something that might be called a dynamical system, using only very simple mathematical ingredients.
    Our aim here is to use simplicity to highlight the core conceptual ingredients; later we will consider more sophisticated formalizations, where this basic structure might be obfuscated by the formalism.

    As a state space we consider the following set $\Obja$, whose elements we think of as some different possible states in the lifetime of a plant (sprout, young, mature, old, dead):
    \begin{equation}
        \Obja =
    \end{equation}
}

\subsection{Discrete-time systems}

Consider for example the monoid $\mathbf{T} = \tup{\natnumbers, +, 0}$ as a discrete model of time.
Observe that $\mathbf{T}$ is generated by the number $1 \setin \natnumbers$: every natural number $n$ can be written as a $n$-fold sum of $1$ with itself.
If we model a dynamical system by a left action operation of $\mathbf{T}$ on a state space $\stylesets{X}$,
\begin{equation}\label{eq:discrete-time-dynamics}
    \dyn \colon  \stylesets{\natnumbers} \cartprod \stylesets{X}   \sto \stylesets{X},
\end{equation}
then in particular this dynamics function is compatible with the summing of natural numbers (which is the composition operation in the monoid $\mathbf{T}$):
\begin{equation}
    \dyn(n, \dyn(m, x) = \dyn(n + m, x).
\end{equation}
Because $\mathbf{T} = \tup{\natnumbers, +, 0}$ is generated by the number $1$, the function \cref{eq:discrete-time-dynamics} is then completely determined by the function
\begin{equation}\label{eq:discrete-dynam-generating}
    \dyn(1, - ) \colon \stylesets{X}   \sto \stylesets{X}.
\end{equation}
If we start ``running'' the dynamical system at some point $x_0$ at time $t = 0$, then iteratively applying the function \cref{eq:discrete-dynam-generating} will compute that trajectory $x_0, x_1, x_2, x_3, \dots$ of the initial point $x_0$ as time progresses.
Often a discrete-time dynamical system is specified by defining the function \cref{eq:discrete-dynam-generating} in the following notation,
\begin{equation}\label{eq:discrete-recursion-dynamics}
    x_{n+1} = \mapa(x_n),
\end{equation}
where $\mapa = \dyn(1, - )$ and $x_n$ denotes $\dyn(n, x_0)$, the $n$-th element of a trajectory starting at some $x_0$.

\begin{example} The logistic equation
    \begin{equation}
        x_{n+1} = rx_n(x_n - 1)
    \end{equation}
    is of the form \cref{eq:discrete-recursion-dynamics}, with $\stylesets{X} = [0,1] \subseteq \reals$ and where $r \setin \reals$ is a parameter.
    Despite being only 1-dimensional and deterministic, this dynamical system can exhibit extremely complex behavior (more precisely: deterministic chaos), depending on which value for the parameter $r$ is chosen.
\end{example}

\subsection{Continuous-time systems}

Consider the initial value problem
\begin{equation}
    \begin{cases}
        \dot{\stylemaps{x}}(t) = \mapa(\stylemaps{x}(t)) \\
        \stylemaps{x}(0) = x_0
    \end{cases}
\end{equation}
where $\mapa \colon \reals^n \sto \reals^n$ is a differentiable function, $\stylemaps{x} \colon \reals \sto \reals^n$ is a continuously differentiable function, and $x_0 \setin \reals^n$.
Under suitable assumptions on $\mapa$, for each choice of $x_0$, there exists a unique solution $\stylemaps{s}_{x_0} \colon \reals \sto \reals^n$ of this initial value problem.
Using all of these solutions, it is possible to define the following map, which is called a \emph{flow}:
\begin{equation}
    \defmapperiod{
        \varphi
    }{
        \reals \cartprod \reals^n
    }{
        \to
    }{
        \reals^n
    }{
        \tup{t,x_0}
    }{
        \stylemaps{s}_{x_0}(t)
    }
\end{equation}

In this case, the flow map $\phi$ will necessarily satisfy the following conditions
\begin{enumerate}
    \item $\varphi(t + s, x) = \varphi(t, \varphi(s,x)) \quad \forall t, s \setin \reals, x \setin \reals^n$,
    \item $\varphi(0, x) = x \quad \forall x \setin \reals^n$,
\end{enumerate}
which means that it is a left action operation on $\reals^n$ by the monoid $\tup{\reals, +, 0}$.

\todotext{JL: explain in a couple more words the intuition/idea, and perhaps include a graphic here}

\begin{definition}
    \label{def:ct-dynsyst}
    A continuous-time dynamical system on~$\reals^n$ is a continuously differentiable function
    \begin{equation}
        \dyn \colon \reals \cartprod \reals^n \to \reals^n
    \end{equation}
    which is a left action operation of the monoid $\tup{\reals, +, 0}$
\end{definition}

\subsection{Event-based systems}

\subsection{Machines}

\subsection{Linear time-invariant systems}

\begin{definition}[LTI System]
    \label{def:LTI_syst_first_time}
    A \emph{linear time-invariant dynamical (LTI) system}, in a state-space representation, is specified by real \SY{vector spaces}~$\prin=\reals^l$ (input space),~$\prout=\reals^m$ (output space), and~$\prst=\reals^n$ (state space), along with a system of equations of the form
    \begin{align}
        \dot{\mat{x}}(t) & = \mat{A}\mat{x}(t) + \mat{B}\mat{u}(t) \label{eq:LTI-system-dyn-equation} \\
        \mat{y}(t)       & = \mat{C}\mat{x}(t) + \mat{D}\mat{u}(t) \label{eq:LTI-system-ro-equation},
    \end{align}
    and an \emph{initial state}~$\prstart\setin \prst$, where~$t \setin \nonNegReals$,~$\mat{u}(t) \setin \prin$,~$\mat{y}(t) \setin \prout$,~$\mat{x}(t) \setin \prst$, and where~$\mat{A}$,~$\mat{B}$,~$\mat{C}$,~$\mat{D}$ are real matrices of appropriate dimension.
\end{definition}

We think of \cref{eq:LTI-system-dyn-equation} as defining dynamics
\begin{equation}
    \defmapcomma{
        \prdyn
    }{ \prin \cartprod  \prst
    }{ \sto
    }{  \prst
    }{ \tup{u, x}
    }{ \mat{A}x + \mat{B}u
    }
\end{equation}
and \cref{eq:LTI-system-ro-equation} as defining a read-out function
\begin{equation}
    \defmapperiod{
        \prreadout
    }{ \prin \cartprod  \prst
    }{ \sto
    }{  \prout
    }{ \tup{u, x}
    }{ \mat{C}x + \mat{D}u
    }
\end{equation}

