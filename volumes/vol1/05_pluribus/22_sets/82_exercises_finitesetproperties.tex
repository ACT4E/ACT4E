\subsection{Properties}

\begin{codeexercise}[\exname{TestFiniteSetProperties}]
  Given two finite sets, check if they are a subset of the other.
  Implement the interface in \cref{lst:FiniteSetProperties}.

\end{codeexercise}

\classlisting{FiniteSetProperties}

As you can see, once you define \funcname{is_subset}, you get for free the implementation
of \funcname{equal} and \funcname{is_strict_subset}.

Check your results with

\begin{console}
  make check-TestFiniteSetProperties
\end{console}
