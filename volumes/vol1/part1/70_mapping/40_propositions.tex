% !TEX root = standalone.tex


\section{\statusdraft{Propositions}}
\label{sec:connection-propositions}
\todotext{Define the category of propositions where objects are propositions
and morphisms are proofs. Also use to introduce the sequent notations}

\todographics{Learn the ebproof package. Example below}

\newcommand{\nameinfer}[1]{\infer[left label=$#1:$]}


\begin{prooftree}
  \hypo{ A }
  \nameinfer{g}1{ B }
  \hypo{ B }
  \nameinfer{f}1{ C }
  \infer2{
    \begin{prooftree}
      \hypo{ A }
      \nameinfer{f \then g}1{C}
    \end{prooftree}
%	  \nameinfer{f\then g }1{ C }
  }
\end{prooftree}
