% !TEX root = chapter-standalone.tex

\devel{

    \section{Monoidal DPs}

    If the underlying posets of functionality and resources are monoidal (\cref{def:monoidal-poset}), then we can define some canonical DPs.

    \todojira{207}{Intro, reference "monoidal poset".}

    \begin{definition}[Sum of resources]
        \label{def:sum-resources}
        If the poset~$\posgenA$ is monoidal, then the ``\emph{sum}'' of~$n$ copies of $\posgenA$ is a design problem given by
        \begin{align*}
            \sum \colon (\posgenA^n)\op \times \posgenA &\toinPos \Bool \\
            \tup{ \F{a_1}^*, \dots, \F{a_n}^*, \R{a_\text{resource}}} &\mapsto (\F{a_1} + \dots + \F{a_n} \posleq_\posgenA \R{a_\text{resource}}).
        \end{align*}
        Clearly $\sum$ is monotone. Diagrammatically:
        \begin{center}
            \includesag{60_dependent}
        \end{center}
    \end{definition}

    We can de
    \begin{definition}[Sum of functionalities for monoidal posets]
        \label{def:sum-functionality}
        If the poset~$\posgenA$ is monoidal, then the ``\emph{sum}'' of~$n$ copies of $\posgenA$ is a design problem given by
        \begin{align*}
            \sum \colon \posgenA \times (\posgenA^n)\op  \posgenA &\toinPos \Bool \\
            \tup{ \F{a_\text{func}}, \R{a_1}^*, \dots, \R{a_n}^*} &\mapsto ( \F{a_\text{func}} \posleq_\posgenA \R{a_1} + \dots + \R{a_n}  ).
        \end{align*}
        Clearly $\sum$ is monotone. Diagrammatically:
        \begin{center}
            \includesag{60_dependent}
        \end{center}
    \end{definition}

    \todojira{208}{Now do sum of functionality, and then the mixed one}

    \todojira{209}{Now show that it is the composition of conjoint and companion of "+".}}